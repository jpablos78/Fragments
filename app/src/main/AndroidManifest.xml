<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jpablos.fragments">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FragmentoEmbebidoActivity"
            android:label="@string/fragmentos_embebidos" />
        <activity
            android:name=".FragmentoProgramacionActivity"
            android:label="@string/fragmentos_programacion" />
        <activity android:name=".FragmentoConBottomNavigationActivity" />
        <activity
            android:name=".Main2Activity"
            android:label="@string/title_activity_main2" />
        <activity android:name=".FragmentoConBottomNavigationLibraryActivity" />
        <activity android:name=".FragmentPassDataActivity"
            android:label="@string/fragmentos_pass_data"></activity>
    </application>

</manifest>